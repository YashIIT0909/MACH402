{
  "name": "@cleargate/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that lets an AI agent shop for and pay for ClearGate metered GPU sessions on its own",
  "type": "module",
  "bin": {
    "cleargate-mcp": "./bin/cleargate-mcp.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "//": "Deliberately standalone: no @cleargate/* workspace dependency. Everything it needs from client/ and packages/types is copied into src/vendor/, so this package can be published and run with no monorepo context at all.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "@hiero-ledger/sdk": "2.85.0",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@x402/core": "2.25.0",
    "@x402/fetch": "2.25.0",
    "@x402/hedera": "2.25.0",
    "dotenv": "17.4.2",
    "tsx": "4.23.13",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "5.9.3"
  }
}
